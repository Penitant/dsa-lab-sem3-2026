# docs/

Static GitHub Pages dashboard listing assignments by week.

- `index.html` — the page; fetches `assignments.json` and renders it. No
  build step, no framework.
- `assignments.json` — auto-generated by `.github/workflows/build-dashboard.yml`
  from `assignments/weekN.md` frontmatter. Never hand-edit.
